import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AppProposPage } from './app-propos';

@NgModule({
  declarations: [
    AppProposPage,
  ],
  imports: [
    IonicPageModule.forChild(AppProposPage),
  ],
})
export class AppProposPageModule {}
